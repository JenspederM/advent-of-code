[project]
name = "advent-of-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jens Peder Meldgaard", email = "jenspederm@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.3.4",
]

[project.scripts]
advent-of-code = "advent_of_code:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "rich>=13.9.4",
]
